# paddy_rice_web

# Run this command to install all necessary library
pip install streamlit SQLAlchemy psycopg2-binary bcrypt PyJWT Pillow reportlab numpy tensorflow-cpu google-genai 

# Run this command to see the web
streamlit run app.py